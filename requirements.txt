# Core
django==5.0.4
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-environ==0.11.2
django-filter==24.2
djangorestframework-simplejwt==5.3.1
dj-database-url==2.1.0

# Auth & MFA
django-otp==1.5.1
qrcode[pil]==7.4.2
python-ldap==3.4.4
django-auth-ldap==4.6.0

# Storage
boto3==1.34.74               # S3 / compatible object storage
django-storages==1.14.2

# Search
elasticsearch==8.12.1
elasticsearch-dsl==8.9.0
django-elasticsearch-dsl==8.0

# Task queue
celery==5.3.6
redis==5.0.3
django-celery-beat==2.6.0
django-celery-results==2.5.1

# WebSocket / Channels
channels==4.0.0
channels-redis==4.2.0
daphne==4.2.1

# Document processing
python-docx==1.1.0
openpyxl==3.1.2
pdfplumber==0.11.0
Pillow==10.3.0
python-magic==0.4.27
PyMuPDF==1.24.14
reportlab>=4.0

# DB
mysqlclient==2.2.4

# Utils
gunicorn==22.0.0
whitenoise==6.6.0
django-auditlog==3.0.0
shortuuid==1.0.13

#ocr
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
opencv-python-headless==4.6.0.66
# boto3>=1.34.0        # only needed when OCR_ENGINE=textract


# PADDLEOCR
# CPU-only. Switch to paddlepaddle-gpu==2.6.2 if your celery_ocr container
# has CUDA drivers available (requires nvidia-docker runtime).
paddlepaddle==2.6.2
paddleocr==2.7.3
Cython<3
protobuf>=3.20.2,<5.0.0
#
# The PP-OCRv4 models (~8 MB) are downloaded on first use to ~/.paddleocr/.
# Pre-download them in your Dockerfile to avoid cold-start latency on first job:
#
#   RUN python -c "from paddleocr import PaddleOCR; \
#       PaddleOCR(lang='en', use_angle_cls=True, show_log=False)"
#
# The docker-compose.yml mounts a named volume at /root/.paddleocr so models
# persist across container rebuilds and aren't re-downloaded on every deploy.

# SPACY NER
spacy==3.7.4
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
#   RUN python -m spacy download en_core_web_sm
#
# Optional larger models (set OCR_SPACY_MODEL accordingly):
#   python -m spacy download en_core_web_lg      # ~560 MB — much better NER
#   python -m spacy download en_core_web_trf     # transformer-based — best accuracy


# DATEUTIL  (already likely installed, add for explicit pinning)
python-dateutil>=2.8.2
# anthropic
anthropic>=0.97.0
pypdfium2>=4.30.0
